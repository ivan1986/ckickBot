[Unit]
Description=Click Bot Messages Processor
After=network.target

[Service]
Type=simple
WorkingDirectory={{ dir }}
ExecStart={{ dir }}/bin/console messenger:consume async
Restart=on-failure
RestartSec=10s
User={{ user }}
Group={{ group }}

[Install]
WantedBy=default.target
