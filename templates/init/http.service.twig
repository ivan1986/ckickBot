[Unit]
Description=Click Bot Http
After=network.target

[Service]
Type=simple
WorkingDirectory={{ dir }}
ExecStart=symfony server:start --no-tls --listen-ip=0.0.0.0
Restart=on-failure
RestartSec=10s
User={{ user }}
Group={{ group }}

[Install]
WantedBy=default.target
