[Unit]
Description=Click Bot Sceduler
After=network.target

[Service]
Type=simple
WorkingDirectory={{ dir }}
ExecStart={{ dir }}/bin/console messenger:consume scheduler_default
Restart=always
User={{ user }}
Group={{ group }}

[Install]
WantedBy=default.target
